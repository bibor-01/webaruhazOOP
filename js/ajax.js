/*class Ajax{
constructor(){
/*this.szuloElem = $("section");
this.sablonElem = $(".termekek");
this.termekek = [];
this.fajlnev = "http://localhost:3000/termekek";
this.beolvas(this.fajlnev, this.termekek);

}
beolvas(fajlnev, tomb) {
  $.ajax({
    url: fajlnev,
    type: "GET",
    success: function (result) {
      result.forEach((element) => {
        tomb.push(element);
      });
      tomb.forEach(function (adat) {
        let ujElem = this.sablonElem.clone().appendTo(this.szuloElem);
        const ujTermek = new TermekGaleria(ujElem, adat);
      });
      
    },
  });
  this.sablonElem.remove();

}

}*/

  